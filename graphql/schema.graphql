type Item {
    uuid: String!
    name: String!
    departure: String!
    destination: String!
    time: String!
    capacity: Int!
    passenger: Int!
}

type Mutation {
    putItem(uuid: String!, name: String!, departure: String!, destination: String!, time: String!, capacity: Int!, passenger: Int!): Item!
}

type Query {
    allItems: [Item!]!
}